# Create state-level aggregate from district data

Create state-level aggregate from district data

## Usage

``` r
create_state_aggregate(df, end_year)
```

## Arguments

- df:

  Processed data frame (should contain district-level rows)

- end_year:

  School year end

## Value

Single-row data frame with state totals
