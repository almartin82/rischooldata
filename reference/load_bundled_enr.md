# Load bundled enrollment data

Loads the bundled October 1st enrollment data from the package. This is
the primary data source since the RIDE Data Center requires
browser-based downloads.

## Usage

``` r
load_bundled_enr(end_year)
```

## Arguments

- end_year:

  School year end (2011-2026)

## Value

Data frame with enrollment data in wide format
